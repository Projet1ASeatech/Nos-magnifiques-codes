#include "Coordonne.h"
#include "Function.h"
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(){
  int numint=1;
  bool running = True;
  while running{
    bool modifie=true;
    while modifie{
      modifie=false;
      change(M);
      affiche(M);
      numint=-numnint
  }
}
